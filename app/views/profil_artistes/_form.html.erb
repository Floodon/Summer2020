<%= form_with(model: profil_artiste, local: true) do |form| %>
  <% if profil_artiste.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(profil_artiste.errors.count, "error") %> prohibited this profil_artiste from being saved:</h2>

      <ul>
        <% profil_artiste.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nom_de_scene %>
    <%= form.text_field :nom_de_scene %>
  </div>

  <div class="field">
    <%= form.label :nom %>
    <%= form.text_field :nom %>
  </div>

  <div class="field">
    <%= form.label :prenom %>
    <%= form.text_field :prenom %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
